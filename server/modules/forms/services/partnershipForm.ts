import { FileRecord } from "#packages/storage";

export const ssPartnershipForm = async (
  field: FileRecord[],
  files: Record<string, any>,
) => {
  console.log("DDDD : ", { field, files });

  return;
};
