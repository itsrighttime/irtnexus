import { BADGE_CATEGORIES, BADGE_KEYS } from "./badgeDefinitions";
import { validateBadges } from "./badgeRules";
import { getMetaForBadge } from "./badgesIconMap";
import { userProfileFields } from "./dummy/userProfileFields";

export const profileHelpers = {
  BADGE_CATEGORIES,
  BADGE_KEYS,
  validateBadges,
  getMetaForBadge,
  userProfileFields,
};
