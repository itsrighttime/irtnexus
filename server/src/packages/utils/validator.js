import {
  validator,
  validatePayload,
  ERROR_KEYS,
  VALIDATOR_KEY,
} from "@itsrighttime/utils";

// ----------- EXPORT UTILS LOGGER -----------

export const UtilsValidator = {
  validator,
  validatePayload,
  Error,
  ERROR_KEYS,
  VALIDATOR_KEY,
};
