// import { emailConsumer } from "./emailConsumer";
// import { smsConsumer } from "./smsConsumer";
// import { pushConsumer } from "./pushConsumer";
// import { inAppConsumer } from "./inAppConsumer";
import { EventBus } from "#packages/event-bus/";

export const notificationConsumers = (eventBus: EventBus) => {
//   emailConsumer(eventBus);
//   smsConsumer(eventBus);
//   pushConsumer(eventBus);
//   inAppConsumer(eventBus);
};
